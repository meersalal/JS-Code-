const obj = {
    js : 'javascript',
    cpp: 'c++',
    rb: 'ruby'
}

for (const key in obj) {
   // console.log(`${key} shortcut is for ${obj[key]}`);
}

const programing = ['js','java','c++']

for (const key in programing) {
    console.log(programing[key]);
}

